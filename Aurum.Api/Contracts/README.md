# Contracts

Shared request/response DTOs and API contracts that are used across more
than one feature module. Feature-specific DTOs should live inside their
own Features/<FeatureName> folder instead.
